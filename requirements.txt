pydantic>=2.6
python-dotenv>=1.0
rich>=13.7
pytest>=8.0

# Optional — only imported if PROVIDER=openai / PROVIDER=anthropic.
# Installed by default so a real-LLM run works immediately given a key,
# with no separate install step.
openai>=1.50
anthropic>=0.40

# Optional — only imported if EMBEDDING_PROVIDER=fastembed (v2.3). ONNX
# runtime, no torch, ~65MB model cache on first use, no API key needed.
# The default EMBEDDING_PROVIDER=hash needs none of this.
fastembed>=0.5
